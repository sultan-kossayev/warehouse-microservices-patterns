spring:
  application:
    name: configserver
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:config/productservice, classpath:config/pricingservice

server:
  port: 8081